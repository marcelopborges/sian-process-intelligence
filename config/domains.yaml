# Domínios para inferência semântica a partir do SX3 (Protheus).
# Usado pelo pipeline semântico (`app.pipeline.runner`, scripts/run_*.py) com --domain <id>.

cp:
  label: "Contas a Pagar"
  description: "Cadeia SE2 → FK7 → FK2 → SE5; mart_cp_event_log e intermediários int_cp_*."
  tables:
    - SE2
    - FK7
    - FK2
    - SE5
  # Opcional: sobrescrever/estender vínculos mart (tabela/coluna) por atividade.
  # Formato: activity -> primary: [{table, column}, ...]
  # mart_override: {}
