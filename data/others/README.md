# Dados locais (não versionados)

Coloque aqui o export do **SX3** em CSV (ex.: `SX3010_*.csv`). Arquivos `*.csv` estão no `.gitignore` por limite de tamanho do GitHub.

Referência de uso: `README.md` (seção governança semântica / SX3).
